#include "sortic.h"

//void reverse_vector(vector <int>& arr) {
//	int left = 0, right = arr.size() - 1;
//	for (int i = 0; i < arr.size() / 2; i++)
//	{
//		int temp = arr[left];
//		arr[left] = arr[right];
//		arr[right] = temp;
//		left++;
//		right--;
//	}
//}